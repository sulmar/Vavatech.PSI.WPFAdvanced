<Window x:Class="Vavatech.PSI.WPF.WPFClient.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Vavatech.PSI.WPF.ViewModels;assembly=Vavatech.PSI.WPF.ViewModels"
        xmlns:local="clr-namespace:Vavatech.PSI.WPF.WPFClient.Views"
        mc:Ignorable="d"
        Title="ShellView" Height="450" Width="800">

    <Window.DataContext>
        <vm:ShellViewModel />
    </Window.DataContext>
    
    <Grid>

        <DockPanel>
            
            <!-- Header -->
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Height="50">
                <Button Width="100">Employees</Button>
                <Button Command="{Binding ShowActivitiesCommand}" Width="100">Activities</Button>
            </StackPanel>


            <!-- Content -->
            <Border BorderBrush="Black" BorderThickness="1">
                <ContentControl Content="{Binding SelectedViewModel}"  />
            </Border>
            
        </DockPanel>
        
    </Grid>
</Window>
